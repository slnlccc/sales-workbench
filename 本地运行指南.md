# 销售工作台 - 本地运行指南

## 环境要求

- **Node.js**：v18.0 或更高版本（推荐 v20 LTS）
  - 下载地址：https://nodejs.org/
- **npm**：随 Node.js 一起安装
- **MongoDB**（可选）：本地数据库
  - 如未安装，系统会自动使用内存数据库运行
  - 下载地址：https://www.mongodb.com/try/download/community

## 安装步骤

### 1. 解压项目

将下载的压缩包解压到任意目录，例如：`D:\sales-workbench`

### 2. 安装前端依赖

打开命令行（Windows 用 cmd 或 PowerShell，Mac/Linux 用 Terminal），切换到项目根目录：

```bash
cd D:\sales-workbench
npm install
```

### 3. 安装后端依赖

```bash
cd server
npm install
cd ..
```

## 启动服务

需要**同时启动两个服务**（建议打开两个命令行窗口）：

### 启动后端服务（窗口1）

```bash
cd server
node src/server.js
```

启动成功后会显示：
```
服务器运行在 0.0.0.0:3001
网络访问模式: 公网开放模式
安全策略: JWT鉴权已启用，IP白名单已关闭
```

### 启动前端服务（窗口2）

```bash
npm run dev
```

启动成功后会显示：
```
VITE v6.x.x  ready in xxx ms

➜  Local:   http://localhost:5174/
```

## 访问工作台

打开浏览器（推荐 Chrome / Edge），访问：

```
http://localhost:5174/
```

## 默认账号

- **用户名**：`admin`
- **密码**：`admin123`

## 可选：构建生产版本

如需部署到服务器，可构建生产版本：

```bash
npm run build
```

构建产物位于 `dist/` 目录，后端服务会自动托管这些文件（端口 3001）。

## 常见问题

### Q: 启动后端时提示端口被占用？

修改 `server/.env` 文件中的 `PORT` 值，例如改为 `3002`，同时更新 `vite.config.ts` 中 proxy 的 target。

### Q: 启动后端时提示 MongoDB 连接失败？

这是正常现象，系统会自动切换到内存数据库。如需持久化数据，请安装 MongoDB 并启动服务。

### Q: 前端页面打不开？

1. 确认前端服务已启动（窗口2 显示 VITE ready）
2. 确认浏览器访问的是 `http://localhost:5174/`（注意是 http 不是 https）
3. 检查命令行是否有错误信息

### Q: 登录后无法加载数据？

确认后端服务已启动（窗口1 显示"服务器运行在 0.0.0.0:3001"）。

### Q: 手机访问？

确保手机和电脑连接同一 WiFi，使用电脑的局域网 IP 访问：
```
http://你的电脑IP:5174/
```
查看电脑 IP 方法：
- Windows：命令行输入 `ipconfig`
- Mac：命令行输入 `ifconfig`
- Linux：命令行输入 `ip addr`

## 项目结构

```
sales-workbench/
├── src/                    # 前端源码
│   ├── components/         # React 组件
│   ├── pages/              # 页面
│   ├── context/            # 上下文
│   ├── services/           # API 服务
│   └── ...
├── server/                 # 后端源码
│   ├── src/
│   │   ├── routes/         # API 路由
│   │   ├── models/         # 数据模型
│   │   ├── controllers/    # 控制器
│   │   └── server.js       # 入口文件
│   └── .env                # 环境配置
├── vite.config.ts          # Vite 配置
└── package.json            # 项目依赖
```

## 技术栈

- **前端**：React + TypeScript + Vite + Tailwind CSS
- **后端**：Node.js + Express + MongoDB
- **认证**：JWT
